# v0.1.2 対応計画書（Python 3.11 引き上げ）

> **対応完了**（2026-05-22）。全タスク・全試験項目を消化済み。E-003（Dependabot #66 の解消確認）のみ、PR マージ後に GitHub Dependabot 画面で最終確認を行う。

- 対象 issue: [#17](https://github.com/elvezjp/spec-code-ai-mapper/issues/17)
- 目的: Dependabot アラート [#66](https://github.com/elvezjp/spec-code-ai-mapper/security/dependabot/66)（`idna < 3.15`, GHSA-65pc-fj4g-8rjx）の解消。v0.1.1 は凍結のため v0.1.2 を新設して対応する。

## 1. 対応タスク

### 1.1 バージョン新設

- [x] `versions/v0.1.1/` を `versions/v0.1.2/` へコピー
- [x] `versions/v0.1.2/backend/pyproject.toml` を更新
  - `version = "0.1.2"`
  - `requires-python = ">=3.11"`
- [x] `versions/v0.1.2/backend/uv.lock` を再生成（`idna 3.16` を取り込み）
- [x] `versions/v0.1.2/frontend/package.json` の `version` を `0.1.2` に更新

### 1.2 ドキュメント更新

- [x] `versions/README.md` に v0.1.2 セクションを追加（v0.1.1 は凍結スナップショットとして保持）
- [x] `CHANGELOG.md` / `CHANGELOG_ja.md` に v0.1.2 セクションを追加（SECURITY 修正の旨）
- [x] `SECURITY.md` / `SECURITY_ja.md` のサポートバージョン表を 0.1.2 に更新
- [x] `README.md` / `README_ja.md` の参照バージョン（`versions/v0.1.1/...` 等）を 0.1.2 に更新
- [x] 追加で更新: `CONTRIBUTING.md` / `CONTRIBUTING_ja.md` のパスと前提条件、`.github/workflows/ci.yml` の `working-directory` と Python matrix、v0.1.2 配下の `spec.md` / `config-file-generator-spec.md` / frontend コード / backend コメント、`versions/README.md` の更新作業チェックリスト

### 1.3 仕上げ

- [x] v0.1.2 backend でテスト一式を実行（`pytest`）→ 167 件 pass
- [x] v0.1.2 frontend でビルド・テストを実行（`npm run build` / `npm run test:run`）→ build 成功、test 119 件 pass
- [x] 後述の試験項目表に沿って動作確認（自動で確認可能な範囲）
- [x] PR 作成 → [#19](https://github.com/elvezjp/spec-code-ai-mapper/pull/19)
- [ ] レビュー・マージ

## 2. 試験項目表（v0.1.2 全体動作確認）

本件の Dependabot 修正だけでなく、v0.1.2 がプロダクトとして正しく動くかを確認する観点で記載する。

凡例: ✅ pass / ⏭️ 未実施（環境・人手確認が必要） / ⏳ 後続（マージ後確認）

### 2.1 正常系

| No | 試験項目 | 前提条件 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|---|
| N-001 | backend が Python 3.11 環境でセットアップできること | Python 3.11 がインストール済み | 1. `cd versions/v0.1.2/backend`<br>2. `uv sync` を実行 | エラーなく依存解決・インストールが完了する | ✅ CPython 3.12.10 で `uv sync` 成功（>=3.11 を満たす） |
| N-002 | backend のテストが全て成功すること | N-001 完了 | 1. `uv run pytest` を実行 | 既存テストが全て pass する | ✅ 167 passed, 1 warning |
| N-003 | backend API サーバが起動し、ヘルスチェックに応答すること | N-001 完了 | 1. `uv run uvicorn app.main:app` でサーバ起動<br>2. `/health` にアクセス | 起動エラーなく待ち受け状態になり、`/health` が 200 を返す | ✅ `/health` が静的ファイル mount に隠蔽されないよう修正し、`tests/test_main.py` で 200 応答を確認 |
| N-004 | frontend がビルドできること | Node 環境セットアップ済み | 1. `cd versions/v0.1.2/frontend`<br>2. `npm install`<br>3. `npm run build` | ビルドエラーなく成果物が生成される | ✅ vite build 成功（1805 modules transformed, 1.64s） |
| N-005 | frontend のテストが全て成功すること | N-004 完了 | 1. `npm run test:run` を実行 | 既存テストが全て pass する | ✅ 13 files / 119 tests passed |
| N-006 | トレーサビリティ・マトリクス生成が動作すること | backend / frontend 起動済み | 1. 画面から設計書・コードをアップロード<br>2. マッピング実行 | マトリクスが生成され Markdown 出力できる | ✅ UI 経由で実機確認 OK（設計書・コードのアップロード → マッピング実行 → Markdown 出力まで動作） |
| N-007 | 構造マッチング（md2map / code2map / add-line-numbers）が動作すること | backend 起動済み | 1. 構造マッチング処理を実行 | 上流リポ最新 main の挙動でセマンティック分割・マッチングが完了する | ✅ UI 経由で実機確認 OK（依存先最新 main でもセマンティック分割・マッチングが完走） |
| N-008 | バージョン表記が 0.1.2 で統一されていること | なし | 1. backend `pyproject.toml`, frontend `package.json`, 各ドキュメントを確認 | いずれも `0.1.2` を指している | ✅ backend/frontend/SECURITY/CHANGELOG いずれも 0.1.2、idna は 3.16 |

### 2.2 異常系

| No | 試験項目 | 前提条件 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|---|
| E-001 | Python 3.10 環境では `uv sync` が拒否されること | Python 3.10 のみ利用可能な環境 | 1. `cd versions/v0.1.2/backend`<br>2. `uv sync` を実行 | `requires-python` 違反のメッセージで失敗する | ✅ `uv sync --python 3.10` で `incompatible with the project's Python requirement: ">=3.11"` エラー |
| E-002 | 不正なフォーマットの設計書アップロードが正しくエラーとなること | backend / frontend 起動済み | 1. 非対応形式のファイルをアップロード | バリデーションエラーが表示され、処理が中断する | ✅ 非対応形式アップロード時にバリデーションエラー表示・処理中断を確認 |
| E-003 | Dependabot アラート #66 が再発しないこと | v0.1.2 リリース後 | 1. GitHub の Dependabot ページを確認 | `idna < 3.15` のアラートが解消されている | ⏳ PR マージ後に GitHub Dependabot 画面で確認 |

### 2.3 モバイル

| No | 試験項目 | 前提条件 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|---|
| M-001 | スマートフォン縦画面で frontend のレイアウトが崩れないこと | iPhone Safari / Android Chrome | 1. デプロイ済み v0.1.2 frontend をスマホで開く | 画面要素が画面内に収まり、横スクロールが発生しない | ✅ 実機確認 OK（画面要素が画面内に収まり、横スクロールなし） |
| M-002 | スマートフォンからファイルアップロード操作ができること | M-001 と同条件 | 1. アップロードボタンをタップしてファイル選択 | ファイル選択ダイアログが開き、選択後に処理が走る | ✅ 実機確認 OK（タップでダイアログが開き、選択後の処理も実行） |
